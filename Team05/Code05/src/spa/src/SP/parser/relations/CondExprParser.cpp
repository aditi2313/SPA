#include "CondExprParser.h"

namespace sp {
  std::unique_ptr<ast::CondExprNode> parse(Lexer& lxr) {
    // check if it's a rel expression
    

    // check if it's a not
    
    // deal with binary case
}
}  // namespace sp
